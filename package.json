{
  "name": "mimo-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use MiMo Code from Claude Code to review code or delegate tasks.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
